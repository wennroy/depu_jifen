# 德扑筹码计分系统 - 环境配置

# 后端服务端口
BACKEND_PORT=8001

# 前端服务端口 (开发模式)
FRONTEND_PORT=3000

# 对外暴露端口 (Nginx/Apache 反向代理端口)
EXPOSE_PORT=80

# 数据库路径 (容器内路径)
DB_PATH=/app/data/poker.db

# 默认初始筹码
DEFAULT_INITIAL_CHIPS=1000

# 密钥 (用于生成 token，生产环境请更换)
SECRET_KEY=change-me-in-production-depu-jifen-2024
