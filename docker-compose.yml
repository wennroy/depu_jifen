services:
  app:
    build: .
    container_name: depu-jifen
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${EXPOSE_PORT:-80}:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/poker.db
      - BACKEND_PORT=8000
